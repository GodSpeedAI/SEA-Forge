Read-only review the uncommitted changes in /home/sprime01/projects/sea-rs/.worktrees/domain-model-validation. Intent: (1) map SeaSourceSet validation failures to ForgeError::Plan { class: "domain_model_error" }; (2) enforce MAX_IMPORT_DEPTH=16 using DomainForge 0.15.0's public resolve_semantic_envelope import graph, without modifying DomainForge; entry depth 0, 16 allowed, 17 denied. Review code/tests for correctness, behavior regressions, error semantics, performance, and spec alignment. The full gate `devbox run -- just context-check && just check && just test` passed after implementation. Return only findings ordered by severity with exact file:line; say 'No findings' if none.