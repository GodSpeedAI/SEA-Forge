# Architectural Invariants

Only properties whose violation breaks product truth, safety, compatibility, or
operability appear here. Status values are `satisfied`, `partial`, `violated`,
or `unverified` at commit `8361f25`.

| ID | Statement | Category | Evidence | Affected components | Current status | Mechanical verifier | Violation consequence | Confidence |
|---|---|---|---|---|---|---|---|---|
| EXT-01 | A supported distribution must complete the representative Workbench journey against real backend behavior; preview and mock-only paths cannot support a completion claim. | Externally required | UX epic `:7-31`, `:601-618`; Workbench plan `:495-520`; mocked harness `playwright.config.ts:3-9` | Package, desktop, host, server, SFWP, kernel | violated | Install on a clean supported host; run packaged E2E through settlement and reuse; inspect committed records | Users receive a product that cannot perform its stated job | High |
| EXT-02 | UI states must distinguish declared, installed, available, validated, demonstrated, degraded, unsupported, and unknown, and link material status to committed sources. | Externally required | UX epic `:23-32`, `:547-565`; frontend contract `:93-101` | Renderer, SFWP views, self-model, capability | partial | Contract/component tests reject invented success and require source refs/freshness | UI overstates assurance or capability | High |
| DOM-01 | Settlement, not process completion, determines success; criteria and evidence must pre-exist execution and support the result. | Domain semantic | `AGENTS.md:89-98`; minimum spec `:7-21`; full spec `:1472-1489`, `:1590-1592` | Planner, runtime, evidence, settlement, case runner, UI | violated on server sandbox episodes; satisfied on minimum CLI | Cross-layer test where exit 0 with unmet criteria settles rejected; every episode resolves its criteria and settlement | False success enters case history and capability memory | High |
| DOM-02 | Every sandboxed case activation is a distinct episode with authority echo, trace, evidence, and settlement. | Domain semantic | `spec-full.md:1438-1444`; shortcut at `case_dispatch.rs:497-608` | Case runner, server, runtime, trace, evidence, settlement | violated | Submit one case item and assert the complete canonical run record and cross-links exist | Case work cannot be audited, settled, recovered, or reused honestly | High |
| DOM-03 | Domain-bound plans resolve a hash-pinned DomainForge model and concept refs before authority. DomainForge produces candidate semantics; SEA Forge owns final authority. | Domain semantic | ADR-001 `:31-73`; `spec-full.md:1447-1451`; adapter `src/lib.rs:110-239` | Planner, DomainForge adapter, authority, records | partial; adapter exists but case dispatch passes no candidate | Conformance case with valid/drifted `.sea` refs; assert pre-authority rejection on drift and final mediator decision | Work executes in an undefined or changed semantic world | High |
| AUTH-01 | Every workload side effect, including workspace creation, follows a committed allow decision. Mandatory decision/audit/ledger recording remains allowed so denied outcomes are complete. Unknown, unavailable, or undecidable authority fails closed. | Authority and safety | `AGENTS.md:89-97`; `spec-full.md:63`; directories created first at `case_dispatch.rs:513-552` | All protected ingress, server, authority, runtime, storage | violated on case sandbox setup | Denial tests assert only required governance records change; no workspace/process/network/workload-store mutation occurs | Unauthorized mutation occurs before governance | High |
| AUTH-02 | One authority fabric governs CLI, server, approvals, agents, projections, transitions, and imports; no renderer, bridge, provider, or sandbox bypass exists. | Authority and safety | minimum spec G8 `:65`; ADR-001 `:60-64`; Workbench guide `:35-51` | CLI, server, Tauri, authority, adapters | partial | Static ingress inventory plus denial/no-side-effect tests for every protected verb; forbid generic bridge invoke | Parallel permission systems produce inconsistent or bypassable decisions | High |
| AUTH-03 | The exact non-cloneable grant binds actor, action, resources, policy, sandbox, environment, and timeout and is consumed by the matching runtime. Isolation never silently weakens. | Authority and safety | `sea-forge-authority/src/lib.rs:80-96,146-179`; `sea-forge-runtime/src/lib.rs:7-66`; `spec-full.md:1432-1436` | Authority, runtime, sandbox, adapters | satisfied for the direct runtime seam; unverified across every ingress | Type checks plus mismatch and unavailable-jail conformance tests | Allowed work can change identity or escape authorized boundaries | High |
| AUTH-04 | Protected requests use a host-resolved actor, role, sponsor, policy, and cell context; missing/conflicting identity blocks work. | Authority and safety | UX epic `:80-103`; current mock `router.tsx:38-64`; approval default path | Renderer, Tauri, SFWP, identity, approvals, authority | violated | E2E submits two actors and verifies recorded binding/SoD; missing identity must deny with no effect | Actions are falsely attributed and SoD can be bypassed | High |
| AUTH-05 | Secrets and ambient parent state never enter records or children except through explicit allowlists; child execution uses argv, minimal env, and timeouts. | Authority and safety | `AGENTS.md:167-187`; minimum spec; runtime and provider negative tests | Runtime, sandbox, providers, logs, evidence | partial; minimal env exists, server request timeout does not | Sentinel-secret scan; argv/no-shell tests; timeout tests; gitleaks | Credential leakage or unbounded execution | High |
| DATA-01 | Governance history is append-only and verifiable; mutable views, JSON caches, SQLite, query caches, and generated projections are rebuildable and never truth. | Data consistency | `AGENTS.md:95-98`; frontend contract `:14-31`; ledger `types.rs:987-1051` | Ledger, case/run files, capability SQLite, SFWP views, UI | partial | Rebuild-to-byte-equivalence, hash-chain/MMR verification, mutation prohibition | History can be rewritten or projections can override evidence | High |
| DATA-02 | A run ID has one canonical locator and complete schema across CLI, case episodes, SFWP views, restart, and UI links. | Data consistency | Case writes `cases/<case>/runs` at `case_dispatch.rs:513-519`; run view reads `runs` at `run_views.rs:352-367`; full spec `:659` | Server, case runner, CLI migration, run views, Workbench | violated | Create a case through SFWP; every emitted run ID must resolve before and after restart | Case links lead to `not_found`; evidence becomes operationally orphaned | High |
| DATA-03 | Durable mutation request IDs are idempotency keys, not only status labels; retry or reconnect cannot execute a mutation twice. | Data consistency | Frontend contract `:1889-1894`; current correlation writes without execution dedupe; UI loses ID at `caseAuthoringMachine.ts:97-128,221-248` | SFWP, request store, host, XState machines | violated | Drop response after commit, retry/recover with same ID, assert one mutation and same terminal response | Duplicate cases, approvals, or side effects after network ambiguity | High |
| API-01 | Rust/server types are canonical for SFWP. JSON Schema and TypeScript/AJV are deterministic committed projections; unknown versions and variants fail cleanly. | API and compatibility | ADR-005 `:13-16,37-75`; frontend guide `:53-74`; conformance schema test | Server, generator, contracts package, bridge, renderer | partial; Rust schema gate works, TS/token routine gates absent | Regenerate all projections and require zero diff in CI; unknown-version/variant tests | Frontend and backend silently disagree or misrender new states | High |
| API-02 | The renderer reaches SFWP only through closed typed Tauri query/command enums; it never reads the socket, `.sea-forge/`, or SQL directly. | API and compatibility | `workbench/AGENTS.md:35-47`; frontend contract `:33-49,93-101` | Renderer, host bridge, SFWP | satisfied | Static import/search gate and bridge enum contract tests | Renderer becomes an unaudited second backend | High |
| API-03 | Existing CLI and SFWP v1 behavior remains compatible; additive variants fail cleanly for old clients and schema changes follow ADR-003. | API and compatibility | ADR-003; SFWP v1; unknown-request conformance | CLI, server, host, generated contracts | partial/unverified as full matrix | Old-client fixtures, protocol negotiation, schema compatibility tests | Upgrades corrupt replay or strand installed clients | Medium-high |
| BUILD-01 | The 19 kernel crates remain synchronous and free of async runtimes and HTTP clients; Tokio/HTTP live only in approved edge adapters. | Build and deployment | `AGENTS.md:67-70`; `justfile:111-146`; ADR-004 `:31-43` | Cargo workspaces, server, agent, Tauri host | satisfied in current manifests; Pass 1 did not directly rerun gate | `devbox run -- just no-async-kernel`; assert standalone Tauri `[workspace]` | Runtime concerns infect deterministic kernel boundaries | High |
| BUILD-02 | Supported product platforms are Linux primary and macOS secondary; unsupported platforms are explicit. Platform-only gates report skipped, never passed. | Build and deployment | `spec-full.md:5,1900-1907`; Tauri config currently says `targets: all` | Sandbox, server transport, packaging, CI | partial | Build/install/E2E on Linux and macOS; reject unsupported target claims | Distribution advertises packages whose transport or sandbox cannot work | High |
| BUILD-03 | A release artifact set contains all runtime dependencies needed for its documented journey and version-matched desktop/server/CLI contracts. Bun is development-only. | Build and deployment | ADR-004 `:87-94`; Workbench plan `:495-520`; no completed server distribution/start procedure exists | Packaging, server, desktop, release | violated | Clean-host installation, artifact inspection, version negotiation, no-Bun assertion | Installed Workbench cannot connect or execute | High |
| GEN-01 | Generated zones are never hand-edited; sources change first, generators run, and committed output is drift-checked. | Generated code | `workbench/AGENTS.md:53-76`; minimum generated-zone denial proof | Rust schema, TS/AJV, tokens, `.ua` graph | partial | One `just` recipe regenerates schema, TS/AJV, and tokens and requires zero diff | Duplicate contract truth and irreproducible builds | High |
| OPS-01 | Invalid `server.yaml` blocks server start; startup verifies socket, policy, sandbox, integrity, notify command, and DomainForge. Reload applies atomically to future dispatches or keeps last-known-good and emits a typed event. | Operational | `spec-full.md:1246-1258`; fallback `main.rs:20-30`; discarded reload `lib.rs:1455-1461` | Server config, startup, authority, events | violated | Invalid-config startup test, unavailable-sandbox test, reload behavior/event test | Server runs under unintended defaults or stale policy | High |
| OPS-02 | Ordinary SFWP requests have a 10-second service timeout except subscriptions, and one long operation cannot starve inspection/event recovery. | Operational | `spec-full.md:1594-1599`; sequential connection handler and no timeout | Server, socket client, dispatch | violated | Hanging-handler integration test plus concurrent status/read query | Workbench becomes unresponsive and cannot recover safely | High |
| OPS-03 | Restart preserves committed truth, reconstructs inspectable state, and marks in-flight/orphaned work explicitly rather than silently losing it. | Operational | UX epic recovery stories; disk-backed views; empty in-memory case map on restart | Server, case runner, ledger, projections, Workbench | partial | Kill during each lifecycle stage, restart, verify valid prefix and explicit recovery state | Operators cannot tell whether work ran or what remains lawful | High |

## Status Summary

The minimum CLI satisfies its narrow lifecycle invariants. The integrated server
and Workbench violate load-bearing episode, identity, idempotency, configuration,
and distribution invariants. Those violations, not missing catalog breadth, are
the roots of completion work.
