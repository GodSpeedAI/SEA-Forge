# SF-003 — Canonical Episode Pipeline

```yaml
id: SF-003
title: Unify case sandbox episodes through the full governed lifecycle
priority: P0
objective: Every sandboxed case episode uses the established authority→trace→evidence→settlement pipeline; workspace creation follows authority; settlement evaluates criteria, not exit code alone.
user_or_system_outcome: A case submitted through the Workbench or server produces the same complete, auditable, settleable records as the proven CLI lifecycle.
why_required: |
  This is the single most serious defect. case_dispatch::execute_sandbox (lines 497-609):
  (1) creates workspace directories BEFORE authority evaluation (AUTH-01 violation),
  (2) passes domainforge_candidate: None (DOM-03 violation),
  (3) settles on exit-zero rather than criteria+evidence (DOM-01 violation),
  (4) emits no trace/evidence records during execution (DOM-02 violation).
current_state_evidence: |
  - case_dispatch.rs:513-521 fs::create_dir_all(&workspace) before engine.evaluate (line 530)
  - case_dispatch.rs:543 domainforge_candidate: None
  - case_dispatch.rs:587-594 settlement = if Allow && Completed && exit_code==0 then Accepted else Rejected
  - case_dispatch.rs:600-604 basis is "execution_completed" or "authority_denied" — no criteria evaluation
  - CONTRAST: cli/src/pipeline.rs:474-559 does per-operation authority WITH domainforge_candidate,
    trace.append(AuthorityEvaluated), evidence.prepare/commit, and criteria-based settlement
canonical_requirements: |
  - spec-full.md:1438-1444 every sandboxed episode is a distinct episode with authority echo, trace, evidence, settlement
  - spec-full.md:1432-1436 exact grant consumed by matching runtime
  - spec-full.md:1447-1451 DomainForge candidate before authority
  - spec-full.md:1472-1489 settlement evaluates criteria and evidence, never exit alone
  - AGENTS.md "Architecture Invariants": authority before side effects; settlement not process completion
  - ARCHITECTURAL_INVARIANTS.md DOM-01, DOM-02, DOM-03, AUTH-01
dependencies: []
blocks: [SF-004, SF-009, SF-010, SF-011]
parallelizable_with: [SF-002, SF-007]
parallelization_class: sequential
estimated_scope: large
affected_layers: [case-runner, server-dispatch, authority, runtime, trace, evidence, settlement, capability]
allowed_paths:
  - crates/sea-forge-server/src/case_dispatch.rs
  - crates/sea-forge-case-runner/src/
  - crates/sea-forge-server/tests/
forbidden_paths: [minimum CLI record/schema changes, new duplicate pipeline, exit-code-only settlement, case_views.rs, run_views.rs]
architectural_invariants: [DOM-01, DOM-02, DOM-03, AUTH-01, AUTH-03, DATA-01, K-01, K-02]
interfaces_changed: |
  - execute_sandbox signature/behavior: authority first, then workspace, then full pipeline
  - SettlementEvent.basis includes criteria evaluation result, not just "execution_completed"
data_or_migration_risk: medium — existing case episode records written by the shortcut path must remain readable; new episodes use the full shape
external_side_effects: workspace directories created only after authority allow
implementation_constraints: |
  - REUSE the canonical pattern from cli/src/pipeline.rs:474-599 and plan_pipeline.rs — do NOT
    build a second pipeline. The services already exist: PolicyAuthorityEngine, JsonlTraceRecorder,
    JsonlEvidenceWriter, settlement crate, LedgerStream.
  - Move fs::create_dir_all AFTER the authority Allow decision. On Deny/Escalate, emit only
    governance records (decision, trace, evidence of denial) — no workspace, no process.
  - Pass DomainForge candidate evidence into AuthorityEvaluation (pipeline.rs:486-498 shows how).
  - Replace exit-code settlement with the settlement crate's criteria evaluation. Exit-zero with
    unmet criteria must settle Rejected (false-success test).
  - Emit trace events (AuthorityEvaluated, ExecutionStarted/Completed) and evidence records
    during execution, as the CLI pipeline does.
  - Preserve record schemas: existing minimum CLI records and P1-P4b proofs must remain green.
acceptance_criteria:
  - "Allow path: workspace created only after committed Allow; trace, evidence, and settlement records exist."
  - "Deny path: zero workspace/process/network/workload-store mutation; only governance records (decision+trace+evidence of denial)."
  - "Escalate path: approval request created; no side effect until resolved."
  - "False success: exit-zero with unmet criteria settles Rejected."
  - "Timeout/nonzero-exit: settles Rejected with complete evidence."
  - "DomainForge drift: pre-authority rejection with typed error."
  - "devbox run -- just proof remains green (P1-P4b)."
verification_commands:
  - "cargo test -p sea-forge-server"
  - "cargo test -p sea-forge-case-runner"
  - "devbox run -- just proof"
  - "cargo test --workspace --all-features --locked --no-fail-fast"
manual_validation: "Submit a case through the server with a deny policy; confirm no workspace dir was created. Submit with exit-zero but failing criteria; confirm Rejected."
failure_symptoms: "Case work cannot be audited; false success enters capability memory; unauthorized workspace created before denial."
rollback_or_containment: "Revert execute_sandbox to the shortcut; all changes are localized to case_dispatch.rs and new tests. Existing records remain valid."
required_artifacts: [denial-no-side-effect-test, false-success-test, domainforge-drift-test, full-episode-cross-link-test]
completion_evidence: "One SFWP-created case emits the same lifecycle quality as a CLI run; P1-P4b green."
```

## Context

The canonical governed lifecycle exists and is proven in the CLI path
(`pipeline.rs`, `plan_pipeline.rs`). The server's case dispatch bypasses it for
sandbox episodes via a local shortcut (`execute_sandbox`). This is not a missing
feature — it is a wiring defect that creates pre-authority side effects and
exit-code settlement, violating the product's central invariants.

## Required outcome

Every sandboxed case episode — whether triggered by CLI, SFWP, or Workbench —
flows through the same authority→trace→evidence→settlement pipeline. No side
effect precedes a committed Allow. Settlement evaluates criteria and evidence,
never exit code alone.

## Current evidence

The shortcut at `case_dispatch.rs:497-609`:
- Lines 513-521: `fs::create_dir_all(&workspace)` and `&artifacts` before
  `engine.evaluate` (line 530).
- Line 543: `domainforge_candidate: None`.
- Lines 587-594: `status = if Allow && Completed && exit_code==0 { Accepted } else { Rejected }`.
- Lines 600-604: `basis = ["execution_completed"]` or `["authority_denied"]`.

The correct pattern at `pipeline.rs:474-599`:
- Per-operation authority evaluation with DomainForge candidate (lines 486-516).
- Trace `AuthorityEvaluated` (lines 517-521).
- Evidence prepare + commit (lines 522-540).
- Decision committed to ledger (lines 543-559).

## Implementation boundaries

- **Sole ownership of `case_dispatch.rs`.** No parallel packet edits this file.
- Reuse `CaseRunner`, `PolicyAuthorityEngine`, `JsonlTraceRecorder`,
  `JsonlEvidenceWriter`, the settlement crate, and `LedgerStream` — do not
  duplicate them.
- Preserve `execute_agent`'s delegation path (it already uses the full
  delegation pipeline with basis reuse).
- Do not change minimum CLI record schemas, ID grammar, or exit codes.

## Acceptance scenarios

1. **Allow + exit-zero + criteria met** → Accepted, with trace, evidence,
   settlement, and capability records all present and cross-linked.
2. **Allow + exit-zero + criteria unmet** → Rejected (false success caught).
3. **Deny** → no workspace/process/network mutation; decision + trace +
   evidence of denial committed.
4. **Escalate** → approval request created, no side effect.
5. **Timeout** → Rejected with timeout evidence.
6. **DomainForge drift** → pre-authority typed rejection.
7. **P1-P4b** → still green.

## Verification procedure

```sh
cargo test -p sea-forge-server
cargo test -p sea-forge-case-runner
devbox run -- just proof
cargo test --workspace --all-features --locked --no-fail-fast
```

## Integration notes

SF-004 (run locator) depends on the record shape this packet produces.
SF-009 (case-to-settled-run slice) depends on the full lifecycle being real.
SF-011 (evidence/reuse) depends on criteria-based settlement producing honest
capability records.

## Known traps

- The dispatch loop in `submit()` (lines 87-380) already manages the `JoinSet`
  and calls `record_completion`. The fix is inside `execute_sandbox`, not the
  loop. Do not restructure the loop.
- `execute_agent` (lines 426-494) is already mostly correct (uses delegation
  pipeline with basis reuse). Focus the fix on `execute_sandbox`.
- Existing case episode records written by the shortcut must remain readable by
  `case_views` and `run_views` — the additive ordinal and schema fields are
  backward-compatible.

## Definition of done

A sandbox case episode through the server produces the same record quality as a
CLI run. Denial leaves no side effect. False success is rejected. P1-P4b remain
green.
