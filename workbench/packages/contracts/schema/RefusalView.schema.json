{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RefusalView",
  "description": "Why nothing can be claimed on this connection, in the same vocabulary a\nrefused protected verb uses — so a client never has to map one set of\nreasons onto another.",
  "type": "object",
  "properties": {
    "error_class": {
      "type": "string"
    },
    "message": {
      "type": "string"
    },
    "next_lawful_action": {
      "description": "The server-owned repair path for this error class.",
      "type": [
        "string",
        "null"
      ]
    },
    "no_side_effect": {
      "description": "Inspect clients can state the effect of a prospective refusal without\nattempting a protected command.",
      "type": [
        "boolean",
        "null"
      ]
    }
  },
  "required": [
    "error_class",
    "message"
  ]
}
